import {useState} from 'react'
import {prompts,replies} from '../data/phoneData'
export default function AIChat(){const [messages,setMessages]=useState([]),[input,setInput]=useState('');const ask=q=>{if(!q.trim())return;setMessages(m=>[...m,{q,a:replies[q]||'I can help interpret your iQOO 15 Legend’s hardware, battery, performance, cameras, and simulated device data.'}]);setInput('')};return <section id="companion" className="ai"><div className="ai-copy"><p className="eyebrow">DEVICE-AWARE ASSISTANT</p><h2>Talk to the <em>twin.</em></h2><p>Ask PhoneSense about your device’s performance, health, and hardware—without digging through settings.</p><div className="orb">✦</div></div><div className="chat glass"><div className="chat-head"><b>P</b><div><strong>PhoneSense AI</strong><small><i/> Knows your device</small></div><span>•••</span></div><div className="chat-body"><div className="message bot">Hi, I’m your iQOO 15 Legend’s digital twin. What would you like to know?</div>{messages.map((m,i)=><div key={i}><div className="message user">{m.q}</div><div className="message bot">{m.a}</div></div>)}</div><div className="suggestions">{prompts.map(p=><button onClick={()=>ask(p)} key={p}>{p}</button>)}</div><form onSubmit={e=>{e.preventDefault();ask(input)}}><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask about your phone..."/><button>↑</button></form></div></section>}
